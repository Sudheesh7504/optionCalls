import { BinaryFormatPipe } from './binary-format.pipe';

describe('BinaryFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new BinaryFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
